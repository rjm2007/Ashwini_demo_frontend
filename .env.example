# Backend API base URL — see PART 3 of deployment.md before filling this in.
# Must be an HTTPS URL. A bare IP or http:// URL will NOT work — every API call
# in this app runs directly in the browser and will be blocked as mixed content.
NEXT_PUBLIC_API_URL=
